# BattleMech Foundry System v0.32.6-alpha.0

- Locks the firing sectors to the rendered token mesh instead of a separately calculated document angle.
- Resynchronizes the firing arc during every visual token refresh.
- Keeps the arc aligned with the displayed front through a complete 360-degree rotation.
- Preserves whole-hex snapping and the grid-aligned two-hex perimeter.
